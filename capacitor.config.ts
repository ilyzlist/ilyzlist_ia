const config = {
  appId: 'com.ilyzlist.app',       // <- l’ID unique de ton app
  appName: 'ilyzlist',             // <- le nom de ton app
  webDir: 'out',                   // <- le dossier de build (on l’ajustera plus tard si besoin)
  bundledWebRuntime: false
};

export default config;
